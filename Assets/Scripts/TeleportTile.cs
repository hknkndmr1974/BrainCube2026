using UnityEngine;

// This component marks a tile as a teleport trigger.
// The actual teleport logic is handled in TumbleController.TriggerTeleport.
// Attach this script to the TeleportTile prefab so it can be identified in the scene.
public class TeleportTile : MonoBehaviour {
    // No additional functionality needed for now.
}
